# Generated assets

This folder is populated at build time by
`app/build/copy-brand-assets.targets`. The `.ico` files here are
`.gitignore`d — do not hand-edit them.

Sources live in `app/assets/brand/icons/`.
